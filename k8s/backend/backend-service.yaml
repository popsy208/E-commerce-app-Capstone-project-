apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ecommerce
spec:
  type: NodePort
  selector:
    app: backend
    version: blue
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001